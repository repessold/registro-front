export interface Documentos {
  bopm: number; // Boletim de Ocorrência PM
  bopc: number; // Boletim de Ocorrência PC
  raiia: number; // Registro de Ações Integradas
  partes: number;// Partes envolvidas
}
